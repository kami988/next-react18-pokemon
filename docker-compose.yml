version: "3"

services:
  nextjs:
    build: ./
    volumes:
      - "kami988.next-react18-pokemon:/repo"
    tty: true
    ports:
      - "3010:3000"
    env_file:
      - ./.env

volumes:
  kami988.next-react18-pokemon:
